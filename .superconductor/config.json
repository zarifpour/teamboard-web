{
  "run": [
    {
      "name": "web",
      "commands": [
        "python3 -m http.server 0 --bind 127.0.0.1 --directory \"$(git rev-parse --show-toplevel)\""
      ],
      "default": true
    }
  ]
}
